// Conditional statements (if, else)
let resultCount = 1000;

if (resultCount > 500) {
    console.log("Many results found!");
} else {
    console.log("Few results found.");
}

// Loops (for loop)
for (let i = 0; i < 5; i++) {
    console.log("Iteration", i + 1);
}
