name: Selenium Tests 

on:
  push: 
    branches:
      - main

jobs:

  run-tests:

    runs-on: ubuntu-latest

    steps:

    - name: Run Selenium tests  
      run: |
         python test_checkbox.py
